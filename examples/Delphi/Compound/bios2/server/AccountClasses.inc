{**********************************************************}
   {                                                          }
   {       Bold for Delphi Stub File                          }
   {                                                          }
   {       Autogenerated file for method implementations      }
   {                                                          }
   {**********************************************************}

//
{$INCLUDE AccountClasses_Interface.inc}

function TAccount.ReceiveQueryFromOwned(Originator: TObject; OriginalEvent: TBoldEvent; const Args: array of const; Subscriber: TBoldSubscriber): Boolean;
begin
  result := inherited ReceiveQueryFromOwned(Originator, OriginalEvent, Args, Subscriber);
end;

function TAccount.MayDelete: Boolean;
begin
  result := inherited MayDelete();
end;

procedure TAccount.CompleteCreate;
  function GenAccountNumber: string;
  var
    num: integer;
  begin
    Randomize;
    Result := '';
    While (length(Result) < 6) do
    begin
       num :=  Trunc(Random(900));
       Result := Result + IntToStr(num);
    end;
  end;
begin
  Randomize;
  Number := GenAccountNumber;
  Credit := 100 + Trunc(Random(90000));
  Total := Credit;
end;



